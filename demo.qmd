---
title: "matplotlib demo"
format:
    html:
        code-fold: true
jupyter: python3
---

# First Section

For a demonstration of a line plot, see @fig-line

```{python}
import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 1)
f = np.sin(2 * np.pi * x)
plt.plot(x, f)
```